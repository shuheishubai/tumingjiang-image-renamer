# 接单监控 Chrome 扩展

用途：监控已登录的闲鱼网页版消息，把当前会话文字和可读取的顾客图片同步到店主的“随身接单台”。

边界：

- 需要电脑开机、Chrome 保持运行，并打开 `https://www.goofish.com/im`。
- 不自动发送消息、收款、退款或点击交易按钮。
- 不绕过验证码、登录验证或平台风险控制。
- 使用专用同步令牌，令牌不能登录网站管理页面。

安装：

1. 打开 Chrome 的“管理扩展程序”页面。
2. 打开“开发者模式”。
3. 选择“加载已解压的扩展程序”，选择本文件夹。
4. 在随身接单台生成同步令牌，粘贴到扩展侧边栏。
